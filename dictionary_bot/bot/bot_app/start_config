from db_helper import check_creations
#from config_host import config_host
from bot import bot as bt
#from MESSAGES import sub_will_be_active, start_server, when_finish_sub
from bot import how_many_client_message
from logger import log

async def start_creation():
    try:
        creation = await check_creations()
    except Exception as e:
        log.warning(f'Ошибка при вызове start_creation :: {e}')    

